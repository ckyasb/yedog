#import "../defs.typ": three-line-table
= 问题二：充电策略参数对寿命衰减的影响分析

== 分析难点与方法框架

问题二的核心难点在于 $C_1$、$Q_1$、$C_2$ 三个参数并非完全独立变化：数据集存在两个设计族——其一是 $Q_1 = 80$ 固定族（`3_6C-80PER`、`4_8C-80PER`、`80PER-3_6C`，仅变 $C_1$/$C_2$），其二是 $C_2 = 4$ 固定族（`5C-67PER`、`5_3C-54PER`、`5_6C-19PER`、`5_6C-36PER`，变 $C_1$/$Q_1$）。若直接做多因素独立回归，会因参数耦合与样本量小（每策略 3\~8 块）而失稳。因此本文采用"分组对比 + 非参数检验 + 含交互项的岭回归 + 偏相关 + 随机森林置换重要性"的组合方法，求解流程见 @fig:flow2。

#figure(
  image("../../figures/fig_flow_q2.pdf", width: 70%),
  caption: [问题二策略参数影响分析流程],
) <fig:flow2>

== 策略间寿命差异显著性

采用 Kruskal–Wallis 非参数检验判断 9 种策略间寿命差异是否显著，并以 10000 次置换检验（permutation test）增强小样本下的稳健性，单因素方差分析（ANOVA）作对照。结果如下：

- Kruskal–Wallis $H = 20.65$，$p = 0.0081 < 0.05$；
- 置换检验 $p = 0.0022$；
- ANOVA $F = 3.24$，$p = 0.0062$。

三种检验方向一致，表明 #strong[不同快充策略间循环寿命差异统计显著]。事后两两 Dunn 检验（Bonferroni 校正）进一步定位差异来源。寿命分组与显著性标注见 @fig:kwbox。

#figure(
  image("../../figures/p2_life_kw_box.pdf", width: 82%),
  caption: [各策略寿命分布与 Kruskal–Wallis 检验结果],
) <fig:kwbox>

== 参数与寿命关系及影响程度

为应对多重共线性，建立含交互项的岭回归模型（因变量取对数寿命以稳定尺度）：

$ ln L = beta_0 + beta_1 C_1 + beta_2 Q_1 + beta_3 C_2 + beta_4 (C_1 Q_1) + beta_5 (C_2 Q_1) + epsilon $

岭回归 $R^2 = 0.453$（优化后采用分段稳态寿命作为因变量，贝叶斯岭回归+2阶多项式，R²_LOO 提升至 0.578）。标准化系数为 $C_1 = +0.424$、$Q_1 = +0.145$、$C_2 = -0.329$、$C_1 Q_1 = -0.067$、$C_2 Q_1 = +0.163$。控制其他变量后的偏相关系数见 表2。

#three-line-table(
  [表2 各参数与对数寿命的偏相关系数（控制其余变量）],
  (auto, auto, auto),
  ("参数", "偏相关 $r$", "$p$ 值"),
  (
    "$C_1$", "+0.518", "0.00014",
    "$Q_1$", "+0.323", "0.024",
    "$C_2$", "-0.409", "0.0036",
  )
)
$C_1$ 与 $C_2$ 的偏相关均显著（$p < 0.01$）。其中 $C_2$ 偏相关为负，表明控制其他变量后第二阶段倍率越高寿命越短，与"中高 SOC 段高倍率加速老化"的物理直觉一致；$C_1$ 偏相关为正，是因高 $C_1$ 在实验设计中多伴随较低的 $C_2$ 或更早的 SOC 切换，其直接效应被设计耦合所掩盖。

为量化各参数的影响程度，建立随机森林回归并以置换重要性度量各因素对寿命方差的解释比例。结果显示 $C_2$ 占 $93.9%$，$C_1$ 占 $5.0%$，$Q_1$ 占 $1.1%$（@fig:imp）。因此 #strong[第二阶段倍率 $C_2$ 是寿命衰减的主导因素]，$C_1$ 影响次之，$Q_1$ 直接影响最小（多通过交互项起作用）。

#figure(
  image("../../figures/p2_importance_bar.pdf", width: 60%),
  caption: [随机森林置换重要性：各参数对寿命方差的解释占比],
) <fig:imp>

参数与寿命的散点及偏相关见 @fig:partial。

#figure(
  image("../../figures/p2_param_partial.pdf", width: 88%),
  caption: [$C_1$、$Q_1$、$C_2$ 与循环寿命关系及偏相关系数],
) <fig:partial>

== 不同 SOC 区间高倍率的衰减特征

为量化"不同 SOC 区间采用较高倍率"对衰减的影响，定义两个高倍率暴露量指标：

$ E_"low" = C_1 dot.c Q_1 quad ("低/中 SOC 段高倍率暴露") $
$ E_"high" = C_2 dot.c (80 - Q_1) quad ("中高 SOC 段高倍率暴露") $

以衰减斜率绝对值 $|k|$ 为因变量，对 $E_"low"$、$E_"high"$ 作回归与相关分析（@fig:soc）：

- $|k| tilde E_"low" + E_"high"$ 线性 $R^2 = 0.081$（受参数耦合影响，单变量拟合较弱）；
- $E_"low"$ 与 $|k|$：$r = -0.28$（$p = 0.054$，边际显著，负向）；
- $E_"high"$ 与 $|k|$：$r = +0.24$（$p = 0.091$，正向）。

#figure(
  image("../../figures/p2_soc_exposure.pdf", width: 88%),
  caption: [SOC 区间高倍率暴露量与衰减斜率关系],
) <fig:soc>

结合问题一短寿命策略的特征可得出机制判断：#strong[中高 SOC 段（$Q_1$ 至 80%）持续高倍率是加速衰减的主因]。典型如 `3_7C-31PER-5_9C` 在 31%\~80% SOC 段以 5.9C 充电，$E_"high" = 289$ 为全数据集最大，衰减斜率最高；而低 SOC 段短时高倍率（高 $E_"low"$）对寿命伤害相对较小，反而有助于缩短充电时间。这与题面"低 SOC 区间采用较大电流有助于缩短充电时间，中高 SOC 区间继续采用高倍率则加剧老化"的描述一致。

== 机制讨论与局限性

从电化学机制看，高充电倍率会加剧极化、产热与副反应（如析锂、SEI 膜增厚），加速容量衰减；中高 SOC 段电池电位更高，叠加高电流更易触发副反应，故 $C_2$ 主导衰减的结论具有物理合理性。

本分析的局限性在于：样本量小（每策略 3\~8 块），$C_1$、$Q_1$、$C_2$ 实验设计耦合（非正交），单变量显著性有限；`NEWSTRUCTURE` 标记的未记录结构差异是重要混杂因素；温度与内阻未作为协变量纳入修正。这些局限在问题三、四中通过特征工程与限制外推范围加以缓解。
