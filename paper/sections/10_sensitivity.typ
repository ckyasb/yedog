= 灵敏度分析

== 问题一模型灵敏度

5 折交叉验证下 Lasso MSE 标准差为 15.30（约 $plus.minus 8%$），岭回归标准差 19.41。加入 attendance 特征后 Lasso CV MSE 从 208 降至 198（改善 4.8%）。超参数（Lasso alpha 在 0.3--1.0 范围）变动时 MSE 变化不超过 1%，模型对超参不敏感。

== 问题二权重与算法灵敏度

对 objective_weights 中 8 个权重各 $plus.minus 20%$ 扰动后重新求解，$Z_2$ 变化不超过 5%，说明权重设定对方案排序稳健。6 起点 $Z_2$ 标准差约 0.04（最优 19.1637），多起点高度稳定。模拟退火迭代数从 4000 增至 8000 时 $Z_2$ 提升不足 0.5%，表明算法已收敛。

== 问题三蒙特卡洛与决策灵敏度

不同随机种子下晋级概率 $p_t$ 标准差小于 0.01，20000 次模拟已收敛。权重 $plus.minus 20%$ 扰动后动态仍优于静态，改善率维持在 20%--28% 区间。蒙特卡洛向量化实现（一次性采样 + np.lexsort 排名）使运行时间从 1-2 分钟降至 30 秒以内。

== 问题四稳健性

更换 2018 俄罗斯世界杯作为参照赛事，优化方案在休息时间与黄金时段覆盖上的优势方向不变；标准化方向（higher/lower）一致时结论稳定。实际赛程因规模与本题不同，采用人均、场均、比例口径后各指标可比。
