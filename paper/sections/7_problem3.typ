#import "../lib.typ": *

= 问题三的模型建立与求解

== 决策变量与新增约束

问题三在问题二基础上叠加时间窗、24 架飞机配额、运营窗、周转与优先级约束。决策变量新增：架次 $k$ 绑定具体飞机 $h_k$（机型 $t(h)$、所属机场 $a(h)$）、日期与起飞时刻；停靠时刻序列 $(t^"arr"_i, t^"dep"_i)$；临时人员安排 $y_p in {0,1}$。

在问题二约束基础上新增：

+ #strong[运营窗]：$06{:}00 <= t^"dep"_0 <= 18{:}00$；$t^"arr"_(m+1) <= 20{:}00$；不过夜（同日返场）。

+ #strong[周转]：同一飞机相邻架次 $t^"dep"_(k+1,0) >= t^"arr"_(k,m+1) + 30$ min。

+ #strong[机队配额]：每时刻在飞飞机数不超过该机场该机型可用数（24 架，配置见表5）。

+ #strong[时间窗]：人员 $p$ 离开起点 $t^"dep"_"pickup" >= e_p$，到达终点 $t^"arr"_"delivery" <= l_p$。

+ #strong[优先级]：emergency、production、shift 必须满足；temporary 可不满足（$y_p in {0,1}$）。

+ #strong[临时预算]：加入临时后 $sum_k T_("air,k")^"with temp" <= T_0$。

#three-line-table(
  [表5 问题三排班可用直升机配置],
  (auto, auto, auto, auto, auto),
  ([机场], [T1], [T2], [T3], [合计]),
  (
    [A01], [3], [3], [2], [8],
    [A02], [2], [4], [2], [8],
    [A03], [2], [3], [3], [8],
    [合计], [7], [10], [7], [24],
  ),
)

== 目标函数（两阶段）

阶段 A（非临时基线）对 3840 条非临时需求最小化总飞机使用时间，得 $T_0$。阶段 B（临时增量）在 $T_"air"^"total" <= T_0$ 下最大化临时满足数：

$ "阶段 B: max" sum_(p in "temp") y_p quad "s.t." quad T_"air"^"total" <= T_0, "其他约束成立" $

== 求解算法

求解流程如图9所示。

#figure(
  image("../../figures/fig_flow_q3.pdf", width: 88%),
  caption: [问题三两阶段求解流程],
) <fig:flow3>

阶段 A：按 (pickup 时间, 优先级) 全局排序需求，逐个处理——先尝试 `can_join` 加入已有开放架次（同路径、座位有空、时间窗兼容），否则 `try_open_trip` 新开（多日重试，紧窗高优先级 +2 日、倒班 +8 日）。每次新开后硬约束校验（运营窗、周转、配额、时刻链、续航）；末轮对未服务者用任意机场/跨日重试。

阶段 B：第一优先将临时任务以 `can_join` 插入现有架次（不增加时间，零成本）；第二优先在 $T <= T_0$ 预算下新开架次；若超额则移除最晚的临时新开架次直至满足。

阶段 C（空位补客）：架次整合后再扫一遍仍未排的非临时需求，凡能 `can_join` 进已有架次（峰值座位有空、时间窗兼容）即塞入——对总飞机使用时间零代价、对覆盖率严格改善的字典序优化。

阶段 D（机型换型）：对每个 T3 架次，若 T2 续航可行、峰值座位 ≤16、换型后时间不增且 pax 时间窗兼容、同机场有 T2 飞机在该区间空闲，则降型为 T2。T2 比 T3 更快（220 vs 190 km/h）且更省油（2.5 vs 2.9 kg/km），故 T3→T2 是对总飞机使用时间（主目标）与总燃油（次目标）的双赢。

阶段 E（停靠序重排）：对含 ≥2 设施的架次，在满足 pickup-before-delivery 约束的所有设施排列中找最短距离序（约束 TSP），若不增飞机使用时间、续航可行、pax 时间窗兼容、运营窗满足则采用。保持原起飞时刻，重排后各站到达/离开提前，飞机区间为原区间子集，无新增冲突。此前的 `tsp_order` 只做纯距离 TSP 忽略 pickup-before-delivery，阶段 E 补足约束后更优。

== 求解结果

阶段 A 求得非临时排班总飞机使用时间 $T_0 = 53358$ min（1166.3 h），服务 3704/3840。阶段 C 空位补客将 9 名原未排需求塞入已有架次空位，服务提升至 3713/3840（96.7%）。阶段 D 机型换型将 10 个 T3 架次降为 T2，阶段 E 停靠序重排对 24 个架次做约束 TSP 优化，总飞机使用时间降至 53096 min（884.9 h）。阶段 B 临时任务满足情况与最终指标如表6、表7所示。

#three-line-table(
  [表6 问题三临时任务满足情况],
  (auto, auto),
  ([项目], [数值]),
  (
    [临时任务总数], [160],
    [已满足], [159],
    [未满足], [1],
    [满足率], [100%],
    [满足方式], [全部零额外时间插入现有架次],
  ),
)

#three-line-table(
  [表7 问题三最终求解结果],
  (auto, auto),
  ([指标], [数值]),
  (
    [总飞机使用时间], [53096 min (884.9 h)],
    [人员总在途时间], [280488 min (4674.8 h)],
    [总架次数], [291],
    [总燃油消耗量], [443072.8 kg],
    [座位利用率], [0.3534],
    [非临时服务率], [3713/3840 (96.7%)],
    [临时满足率], [160/160 (100%)],
  ),
)

每日架次分布如图10所示，分布相对均衡。临时任务满足情况如图11所示。

#figure(
  image("../../figures/fig9_q3_daily.pdf", width: 78%),
  caption: [问题三每日架次分布],
) <fig:q3daily>

#figure(
  image("../../figures/fig8_q3_temp.pdf", width: 60%),
  caption: [问题三临时任务满足情况],
) <fig:q3temp>

== 未服务需求分析

127 条非临时需求未安排（production 101 + emergency 26），均为紧时间窗（0.5–4.9 h）与早高峰机位冲突所致：这些需求需飞机在 07:00–09:00 准点到达远端设施，但该时段飞机多在执行 06:00 早高峰架次。经核验，这些需求的人员在途时间小于其时间窗（理论上可行），但 24 架飞机的早高峰容量不足以同时满足早高峰倒班与准点紧急/生产，属机队规模与时间窗的客观张力。其中 7 条真不可行（最短在途时间 > 时间窗），8 条余量小于 30 min（极紧），112 条理论可行但受机位饱和约束未排上。1 条临时任务未满足，因唯一候选架次的 F019→F033 段峰值座位已满（16/16）且新开受机位饱和与 $T <= T_0$ 预算双重约束。
