= 问题三：第三轮动态资源优化

== 问题设定与公式链

第三轮 24 场赛程由问题二固定，仅利用前两轮实际反馈（比分、xG、红牌、伤病、现场观众与转播人数）更新信息并重配资源。每场均基于第三轮开始前的同一信息截面，互不使用其他第三轮结果。题面给出 14 条公式，本文逐式实现。

*球队竞技状态*（公式 1）与伤病（公式 2）：设球队 $t$ 前两轮已赛场数 $G_t$、积分 $P_t$、累计预期进球 $"xGF"_t$、累计预期失球 $"xGA"_t$、红牌 $"RC"_t$，

$
S_t = 0.45 P_t / (3 G_t) + 0.35 [0.5 + 0.5 tanh((("xGF"_t - "xGA"_t)/G_t)/1.25)] + 0.20 exp(-0.55 "RC"_t / G_t)
$

$S_t in [0,1]$；伤病 $h_t$ 取前两轮所涉比赛伤病等级均值并限制在 [0,3]。

*更新进球均值*（公式 3）：令 $Delta_i = 0.32(S_a - S_b) - 0.055(h_a - h_b)$，

$
lambda_("i,a") = "clip"[lambda_(0,i,a) exp(Delta_i - 0.04 h_a), 0.15, 4.50]
$

$lambda_("i,b")$ 对称（$Delta$ 取负、$h_b$）。$lambda_0$ 来自 base_predictions。

*蒙特卡洛模拟*（公式 4）：24 场同时按泊松均值模拟 20000 次，按积分、净胜球、总进球排名；每组前 2 名与 12 组中成绩最好的 8 个第三名晋级，指标完全相同时等比例处理；$p_t = "晋级次数"/20000$。

*晋级重要性*（公式 5）：$Q_i = [4 p_a(1-p_a) + 4 p_b(1-p_b)] / 2$。

*反馈系数*（公式 6）：对球队 $t$ 每场前两轮比赛，实际/赛前现场比与转播比分别 clip[0.60,1.50]，两场均值再 clip[0.75,1.25] 得 $r_t^a, r_t^b$；$f_i^a = sqrt(r_a^a r_b^a)$，$f_i^b = sqrt(r_a^b r_b^b)$。

*更新吸引力*（公式 7）：令 $S_i=(S_a+S_b)/2$、$H_i=(h_a+h_b)/6$、$A_(0,i)="attractiveness"_i/100$、$F_i="clip"[(0.5 f_i^a+0.5 f_i^b-0.75)/0.50,0,1]$，

$
A_i = "clip"[0.50 A_(0,i) + 0.25 Q_i + 0.12 F_i + 0.08 S_i + 0.05(1-H_i), 0, 1]
$

*更新现场需求*（公式 8）：$Ñ_i = N_("i,pre") f_i^a (0.88+0.27 Q_i)(0.94+0.12 S_i)(1-0.10 H_i)$，其中 $N_("i,pre")$ 取问题二 expected_attendance。

*票价调整*（公式 9）：选交通等级读需求修正系数 $m_i^d$，

$
N_i(delta_i) = min{K_i, Ñ_i m_i^d (1+delta_i)^(1/epsilon_i)}, quad "TV"_i = P_(0,i)(1+delta_i) N_i(delta_i)
$

约束 $N_i(delta_i) >= 0.88 N_i(0)$（同交通等级仅 $delta$ 不同）。

*更新转播需求*（公式 10）：$Ṽ_i = V_("i,pre") f_i^b (0.87+0.30 Q_i)(0.90+0.20 A_i)(1-0.06 H_i)$，选转播优先级读 $m_i^b$，$V_i = Ṽ_i m_i^b$，$"BV"_i = u_i V_i$。$V_("i,pre")$ 取问题一 72 场预测。

*风险*（公式 11--14）：无激励风险 $R_("i,noeff")=0.5(2p_a-1)^2+0.5(2p_b-1)^2$；由蒙特卡洛统计条件晋级概率得默契风险 $R_("i,coll")="clip"[D_i(1-"clip"(G_i,0,1))(1-0.35 R_("i,noeff")),0,1]$；动态安保需求 $d_i="clip"[0.45 d_(0,i)+0.25 O_i+0.15 A_i+0.15(R_("i,noeff")+R_("i,coll"))/2,0,1]$，$O_i="clip"(Ñ_i/K_i,0,1)$；选安保等级读剩余风险系数 $m_i^s$，综合风险 $R_i=0.40 R_("i,noeff")+0.40 R_("i,coll")+0.20 d_i m_i^s$；成本 $C_i=c_i^b+c_i^s+c_i^d$。

== 目标函数与求解

$
"Max" Z_3 = sum_(i in I_3) [0.35 "TV"_i^* + 0.35 "BV"_i^* + 0.10 A_i^* - 0.10 C_i^* - 0.10 R_i^*]
$

带星号为 min-max 归一化，上下界取静态与动态候选决策的并集。决策为每场转播优先级、安保等级、交通等级与 $delta_i$，受每日高等级容量、预算、$delta$ 上下限与 $N(delta)>=0.88 N(0)$ 约束。因目标对各场可加、约束除每日容量外分场独立，采用分场枚举离散决策 + $delta$ 一维优化 + 每日容量耦合调整的分解策略。求解流程如图 14 所示。

#figure(
  image("../../figures/fig_flow_q3.pdf", width: 68%),
  caption: [图 14 问题三求解流程：14 式公式链与蒙特卡洛],
)

== 求解结果

蒙特卡洛 20000 次后晋级概率 $p_t$ 范围 0.16--0.97，分布如图 15 所示，多数球队集中在 0.4--0.8 的悬念区间。

#figure(
  image("../../figures/p3_advance_prob_hist.pdf", width: 62%),
  caption: [图 15 问题三球队晋级概率分布],
)

动态方案 $Z_3 = 7.92$，静态方案 $Z_3 = 6.64$，动态改善 19.27%。24 场静态与动态净效益对比如图 16 所示，全部场次动态净效益不低于静态。风险结构（无激励风险与默契风险）如图 17 所示。

#figure(
  image("../../figures/p3_static_vs_dynamic.pdf", width: 92%),
  caption: [图 16 问题三静态与动态净效益对比],
)

#figure(
  image("../../figures/p3_risk_decomposition.pdf", width: 92%),
  caption: [图 17 问题三风险分解],
)

决策范围：转播优先级 1--3、安保等级 1--4（不超过场馆能力且不低于需求）、交通等级 1--3、票价调整 $delta in [-0.15, 0.0]$（受 $N(delta)>=0.88 N(0)$ 与每日上下限约束，以降价提升上座为主）。输出 24 场第三轮动态策略（21 列）。
