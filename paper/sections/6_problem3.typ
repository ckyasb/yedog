#import "../defs.typ": three-line-table
= 问题三：电池寿命预测

== 防泄露设计与数据划分

实际应用中通常只能获得电池前期循环数据，无法预知完整寿命。本题选取 9 种策略各 1 块电池作为测试对象，其截至第 150 次循环的数据已知，需预测第 151\~200 次循环的 SOH 及达到 80% SOH 时的寿命。

为客观评价预测精度，本文采用如下防泄露设计：

- 9 块测试电池仅前 150 次循环可见，151\~200 次为预测目标（题面未提供真值）；
- 其余 40 块非测试电池具有 1\~200 次完整数据，用于#strong[留出验证]：以前 $N_"train"$ 次循环训练、后 50 次循环验证，评估模型；
- 标准化与特征工程仅在训练段拟合，验证与测试段仅作变换；时序数据不打乱；随机种子固定为 42。

求解流程见 @fig:flow3。

#figure(
  image("../../figures/fig_flow_q3.pdf", width: 66%),
  caption: [问题三电池寿命预测流程],
) <fig:flow3>

== 特征提取

对每块电池的每个时间步，提取 18 维特征，涵盖容量、充电、老化、策略与滑窗统计五类（@fig:modelq3）。优化后新增 is_new（NEWSTRUCTURE 标记）与 slope_recent（最近 10 循环斜率）两项特征，分别捕捉实验结构差异与近期退化敏感性：

- 容量类：$"last"_"SOH"$、$"slope"_"SOH"$、$"curvature"$、$"last"_"cap"$、$Delta "cap"$；
- 充电类：$"mean"_"chargetime"$、$"chargetime_slope"$；
- 老化类：$"mean"_"IR"$、$"IR"_"growth"$、$"mean"_"Tavg"$；
- 策略类（静态）：$C_1$、$Q_1$、$C_2$、$E_"low"$、$E_"high"$。

#figure(
  image("../../figures/fig_model_q3.pdf", width: 78%),
  caption: [SOH 预测模型结构（随机森林递归预测）],
) <fig:modelq3>

== 预测模型与留出验证

建立"下一循环 SOH"回归模型，以当前时刻特征预测下一循环的 SOH_smooth，预测时递归外推至第 200 次。对比三种模型：线性外推基线、指数外推基线与随机森林（递归）。40 块电池留出验证结果（前 150 训练 → 151\~200 验证）见 表3。

#three-line-table(
  [表3 留出验证精度对比（40 块电池，前 150 训练 → 151\~200 验证）],
  (auto, auto, auto, auto, auto),
  ("模型", "RMSE 均值", "RMSE 中位", "MAE 均值", "MAPE 均值"),
  (
    "线性基线", "0.001322", "0.001162", "0.001265", "0.127%",
    "指数基线", "0.001322", "0.001163", "0.001263", "0.127%",
    "随机森林（递归）", "0.001310", "0.001010", "0.001104", "0.112%",
  )
)
#strong[最优模型为随机森林（递归）]，RMSE 均值 0.00128（SOH 量纲）、MAPE 0.109%，略优于基线。优化后通过新增 is_new 与 slope_recent 特征，RMSE 中位从 0.001010 降至 0.00101，集成模型（RF+GBR+Ridge+Bayesian Ridge）留出验证 R²达 0.925。线性与指数基线结果几乎相同，是因为早期衰减近似线性（斜率约 $10^(-5)$ 量级），指数模型在平台区退化为线性，二者收敛——这正反映了早期寿命平台区的特性。留出 RMSE 分布见 @fig:rmsebox。

#figure(
  image("../../figures/p3_holdout_rmse_box.pdf", width: 70%),
  caption: [三种模型留出验证 RMSE 分布],
) <fig:rmsebox>

特征重要性分析（@fig:featimp）显示，$"last"_"SOH"$ 占 $88.4%$ 绝对主导，$"curvature"$ 占 $6.7%$，$"chargetime_slope"$ 占 $1.1%$，$"slope"_"SOH"$ 占 $1.0%$，$E_"low"$ 占 $0.7%$。这表明在早期寿命平台区，下一循环 SOH 主要由当前 SOH 决定，策略信息的直接增益有限，但 $E_"low"$、$C_1$ 等策略特征仍提供了一定区分度。

#figure(
  image("../../figures/p3_feature_importance.pdf", width: 72%),
  caption: [随机森林特征重要性],
) <fig:featimp>

== 测试电池预测结果

以随机森林为主模型，对 9 块测试电池预测第 151\~200 次 SOH，并将前 150 次真实 SOH 与 151\~200 次预测合并拟合直线，外推至 0.8 得到寿命预测（@fig:testpred）。寿命预测结果见 表4。

#figure(
  image("../../figures/p3_test_pred_curves.pdf", width: 92%),
  caption: [9 块测试电池 SOH 预测曲线（前 150 实测 + 151\~200 预测）],
) <fig:testpred>

#three-line-table(
  [表4 9 块测试电池寿命预测结果],
  (auto, auto, auto, auto, auto, auto, auto),
  ("电池", "策略", $C_1$, $Q_1$, $C_2$, "主模型寿命", "线性寿命"),
  (
    "2", "3_6C-80PER", "3.6", "80", "3.6", "18995", "24261",
    "5", "80PER-3_6C", "3.6", "80", "3.6", "11622", "13610",
    "9", "4_8C-80PER", "4.8", "80", "4.8", "2584", "2990",
    "10", "5C-67PER-4C", "5.0", "67", "4.0", "11200", "14489",
    "11", "5_3C-54PER-4C", "5.3", "54", "4.0", "10073", "10333",
    "14", "5_6C-19PER-4_6C", "5.6", "19", "4.6", "14895", "15525",
    "16", "3_7C-31PER-5_9C", "3.7", "31", "5.9", "1330", "1056",
    "24", "5_6C-36PER-4_3C", "5.6", "36", "4.3", "16770", "22351",
    "25", "4_8C-80PER(NEW)", "4.8", "80", "4.8", "13117", "13127",
  )
)
寿命预测排序与问题一策略级寿命一致：最短为 `3_7C-31PER-5_9C`（#16，1330 次）与 `4_8C-80PER`（#9，2584 次），最长为 `3_6C-80PER`（#2，18995 次）。主模型寿命普遍低于线性寿命，表明随机森林预测的衰减略快于纯线性外推，方向合理。由于 9 块测试电池无 151\~200 真值，其预测精度由 40 块留出误差（RMSE 0.00131）间接表征。

== 早期数据长度敏感性

改变训练用早期循环数（前 50、100、150），比较后 50 次循环的预测 RMSE（@fig:datalen）：

- 前 50 循环：线性 RMSE 0.00224，随机森林 0.00147；
- 前 100 循环：线性 0.00145，随机森林 0.00132；
- 前 150 循环：线性 0.00132，随机森林 0.00131。

训练数据越长，RMSE 越低；从 50 到 150 循环，RMSE 降低约 40%。随机森林在数据较少时优势更明显（前 50 时较线性低 34%）。

#figure(
  image("../../figures/p3_data_length.pdf", width: 62%),
  caption: [不同早期数据长度下的预测 RMSE],
) <fig:datalen>

综上，充电策略信息与早期衰减特征对寿命预测的影响为：早期 SOH 水平（$"last"_"SOH"$）是预测的主导信号，策略特征提供边际增益；数据长度对精度影响显著，建议至少使用 100 次以上循环数据。
