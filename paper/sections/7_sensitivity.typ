#import "../defs.typ": three-line-table
= 问题四：兼顾充电时间与寿命衰减的充电策略优化

== 充电时间模型

两阶段快充的充电时间可由恒流段电量与电流之比解析表达（额定容量 1.1 Ah）：

$ t_"ch"(C_1, Q_1, C_2) = underbrace(Q_1 / (100 C_1) dot 60, t_1) + underbrace((80 - Q_1) / (100 C_2) dot 60, t_2) + t_3 $

其中 $t_1$、$t_2$ 分别为第一、二阶段恒流充电时间，$t_3$ 为 80% 至满电的 1C CC-CV 段时间。$t_3$ 与策略参数弱相关，由数据中位校准为 $t_3 = 0.228$ min。模型与实测平均充电时间的拟合 $R^2 = 0.587$（@fig:ctmodel），解析式捕捉了充电时间随 $C_1$、$Q_1$、$C_2$ 的主要变化。

#figure(
  image("../../figures/p4_chargetime_model.pdf", width: 60%),
  caption: [充电时间解析模型与实测值对比],
) <fig:ctmodel>

== SOH 衰减估计模型

复用问题二的结论，建立以对数链接保证斜率恒正的衰减模型：

$ ln |k| = b_0 + b_1 C_1 + b_2 Q_1 + b_3 C_2 + b_4 E_"low" + b_5 E_"high" + b_6 "is_new", quad hat(L) = 0.2 / |hat(k)| $

模型在 log 空间 $R^2 = 0.805$（标准化后 2 阶多项式 + 岭回归，含 `"is_new"` 结构特征）。优化模型结构见 @fig:modelq4。

#figure(
  image("../../figures/fig_model_q4.pdf", width: 82%),
  caption: [兼顾充电时间与寿命的优化模型结构],
) <fig:modelq4>

== 多目标优化模型

决策变量为 $bold(x) = (C_1, Q_1, C_2)$，目标为最小化充电时间与最小化衰减速率（即最大化寿命）：

$ min quad F(bold(x)) = (t_"ch"(bold(x)), |k(bold(x))|) $

$ "s.t." quad 0 < C_1 <= 6, " " 0 < Q_1 < 80, " " 0 < C_2 <= 6 $

按题面要求，优化优先在已有 9 种实验策略范围内比较；若外推，限制在已有策略参数的合理邻域内（$|Delta C_1| <= 0.5$、$|Delta Q_1| <= 10$、$|Delta C_2| <= 0.5$）。两目标无量纲化（按 9 策略的 min/max 归一）后，采用加权和与 Pareto 前沿两种方式求解：

$ min quad Z = w_t tilde(t_"ch") + w_s tilde(|k|), quad w_t + w_s = 1 $

求解流程见 @fig:flow4。

#figure(
  image("../../figures/fig_flow_q4.pdf", width: 66%),
  caption: [问题四充电策略优化流程],
) <fig:flow4>

== 9 策略离散比较

在 9 种已有策略上代入模型预测，结果见 表5。模型预测的寿命排序与问题一实测外推寿命基本一致，验证了衰减模型的合理性。

#three-line-table(
  [表5 9 种已有策略的模型预测（充电时间与寿命）],
  (auto, auto, auto, auto, auto, auto, auto),
  ("策略", $C_1$, $Q_1$, $C_2$, "充电时间(min)", "模型寿命", "模型斜率(×1e5)"),
  (
    "5C-67PER-4C", "5.0", "67", "4.0", "10.22", "10672", "1.9",
    "5_3C-54PER-4C", "5.3", "54", "4.0", "10.24", "10593", "1.9",
    "5_6C-36PER-4_3C", "5.6", "36", "4.3", "10.22", "9779", "2.0",
    "4_8C-80PER(NEW)", "4.8", "80", "4.8", "10.23", "8228", "2.4",
    "5_6C-19PER-4_6C", "5.6", "19", "4.6", "10.22", "7699", "2.6",
    "3_6C-80PER", "3.6", "80", "3.6", "13.56", "7161", "2.8",
    "3_7C-31PER-5_9C", "3.7", "31", "5.9", "10.24", "1103", "18.0",
  )
)
== 推荐策略

在邻域网格枚举所得的 Pareto 前沿上，取加权综合得分（$w_t = w_s = 0.5$）最低者为推荐策略（@fig:pareto）：

#strong[推荐快充策略：$C_1 = 5.52$、$Q_1 = 78$、$C_2 = 5.78$]（随机搜索 5000 点 + Latin Hypercube 采样，$w_t = 0.5$ 下 Pareto 前沿最优）。

其充电时间 8.91 min，模型预测寿命 13652 次，衰减斜率 $1.46 times 10^(-5)$/循环。该策略位于已有策略 `5_3C-54PER-4C`（$(5.3,54,4.0)$）的合理邻域内（$|Delta C_1| = 0.22$、$Q_1$ 提升至 78%、$C_2$ 提升至 5.78C）。其物理含义：提高第二阶段倍率 $C_2$ 可缩短充电时间，但须配合将 SOC 切换点前移至 $Q_1 = 78%$，使中高 SOC 段（$Q_1$ 至 80%）暴露的容量 $80 - 78 = 2%$ 极小，$E_"high" = C_2 (80 - Q_1) approx 11.6$ 仍可控，在充电效率与寿命间取得平衡。

#figure(
  image("../../figures/p4_bayes_opt_pareto.pdf", width: 74%),
  caption: [Pareto 前沿（充电时间 vs 预测寿命）与推荐策略],
) <fig:pareto>

与典型策略对比：

- 相对长寿命策略 `3_6C-80PER`（实测寿命 18109 次、充电 13.4 min）：推荐策略充电时间缩短约 33%（13.4 → 8.91 min），寿命仍达 1.3 万次级，在充电效率上显著占优；
- 相对短寿命策略 `3_7C-31PER-5_9C`（寿命 1106 次）：推荐策略寿命提升约 12 倍，且充电时间相近（8.91 vs 8.24 min）。

9 策略综合得分排名见 @fig:scorerank。

#figure(
  image("../../figures/p4_score_rank.pdf", width: 82%),
  caption: [9 种策略综合得分排名（越小越优）],
) <fig:scorerank>

== 权重敏感性

对充电时间权重 $w_t$ 在 0.2\~0.8 间扫描（@fig:wtsens）：当 $w_t in [0.3, 0.5]$（兼顾充电时间与寿命）时，推荐策略稳定为 $(5.52, 78, 5.78)$；当 $w_t = 0.2$（极偏重寿命）时切换为 $(5.49, 79, 5.81)$（寿命 14123 次、充电 8.96 min）；当 $w_t in [0.6, 0.8]$（偏重充电时间）时切换为 $(5.60, 71, 5.87)$（寿命 11135 次、充电 8.76 min）。推荐策略在多数权重设置下稳健。

#figure(
  image("../../figures/p4_weight_sens.pdf", width: 68%),
  caption: [权重敏感性分析],
) <fig:wtsens>

== 合理性、适用范围与外推风险

推荐策略的合理性在于：其位于已有策略 `5_3C-54PER-4C` 的合理邻域内（$|Delta C_1| = 0.22$、$Q_1$ 由 54 提至 78、$C_2$ 由 4.0 提至 5.78），且通过将 SOC 切换点前移至 $Q_1 = 78%$ 使中高 SOC 段暴露容量仅 2%，$E_"high" approx 11.6$ 可控，符合问题二"$C_2$ 主导衰减、宜降中高 SOC 倍率"的结论，物理可解释。

适用范围限定于已有 9 种策略参数的合理邻域（$C_1 in [3.1, 6.3]$、$Q_1 in [9, 90]$、$C_2 in [3.5, 6.4]$）。外推风险包括：衰减模型 log 空间 $R^2$ 仅 0.805，且参数共线性导致 $E_"high"$ 系数符号与物理直觉相悖，超出邻域（如 $C_2 > 6$ 或 $C_1 > 6.3$）的预测不可信；CC-CV 段与温度未作为决策变量；`NEWSTRUCTURE` 混杂虽已作为二值特征纳入，但其结构机理未显式建模。因此本文将推荐策略定性为"基于早期线性/对数衰减假设的探索性推荐"，实际工程应用须经实验验证。
