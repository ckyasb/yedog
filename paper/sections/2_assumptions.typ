= 模型假设

结合赛题数据特点与建模需要，本文做出以下假设：

+ 假设各电池放电策略一致，循环寿命差异主要源于充电策略 $(C_1, Q_1, C_2)$ 的不同，放电条件不作为变量。

+ 假设 80% SOH 为统一的寿命终止阈值，达到该阈值即视为寿命终止。

+ 假设电池早期（前 150\~200 次循环）SOH 衰减近似满足线性或指数趋势，可据此外推至 80% SOH 得到循环寿命。由于实验数据中 49 块电池在观测终点 SOH 仍处于 0.95\~1.00，无一真正触及 0.8，寿命必须由衰减趋势外推获得。

+ 对 `battery_summary.csv` 中 `80PER_3_6C` 策略第一阶段倍率 $C_1$ 缺失的情况，按其策略语义（以 3.6C 充至 80% SOC）补全 $C_1 = C_2 = 3.6$，而非删除样本。

+ 假设问题三中 9 块测试电池的 151\~200 次循环真值不可获取，其预测精度由其余 40 块电池的留出验证间接表征。

+ 假设问题四的策略优化优先在已有 9 种实验策略参数范围内比较；若外推，仅限于已有策略参数的合理邻域（$|Delta C_1| <= 0.5$、$|Delta Q_1| <= 10$、$|Delta C_2| <= 0.5$），超出此范围的预测不作为可信结论。

+ 假设 CC-CV 末段充电时间 $t_3$ 与策略参数相关，由岭回归 + 2 阶多项式拟合 $t_3 = f(C_1, Q_1, C_2)$（$R^2 = 0.884$），而非取常数。
