#import "../lib.typ": *

= 问题二的模型建立与求解

== 决策变量与新增约束

问题二含 4000 条需求（出海 1600 + 海返 1600 + 穿梭 800），无时间窗，飞机充足。同一架次内三类人员混合上下机，座位动态复用。新增决策变量：人员 $p$ 在停靠 $s_i$ 的上机 $u_(p,i)$ 与下机 $v_(p,i)$。

在问题一约束基础上新增：

+ #strong[三类人员起终点匹配]：出海 origin $in {a_k, "LAND"}$、dest $in sigma_k$，pickup=0、delivery=首次到 dest；海返 origin $in sigma_k$、dest $in {a_k, "LAND"}$，pickup=origin 停靠、delivery=末（机场）；穿梭 origin、dest $in sigma_k$，pickup=origin 停靠、delivery=其后首次到 dest。

+ #strong[先下后上]：同一停靠先处理下机再上机，释放座位即时复用。

+ #strong[座位容量时序]：$n_i <= C_(t_k), forall i$，其中 $n_i = n_(i-1) - "下机"_i + "上机"_i$。

== 目标函数

分层目标同问题一。注意穿梭增加绕行可能增大 $T_"pax"$，需在第二层平衡。

== 求解算法

求解流程如图8所示。核心为以设施为中心的往返环构造与座位复用。

#figure(
  image("../../figures/fig_flow_q2.pdf", width: 80%),
  caption: [问题二求解流程],
) <fig:flow2>

+ #strong[需求配对]：同一设施 $F$ 的出海（dest=$F$）与海返（origin=$F$）共享往返环 $A -> F -> A$，座位往返复用（seg0 载出海、seg1 载海返，先下后上）。

+ #strong[构造]：每设施选机型，按 $max(ceil(n_o / C_t), ceil(n_r / C_t))$ 趟构造往返环；穿梭 $F_1 -> F_2$ 单独构造 $A -> F_1 -> F_2 -> A$。

+ #strong[ALNS 优化]：邻域含换机场（LAND 任选）、调整停靠序（2-opt）、穿梭嵌入位置、合并/拆分架次；续航门控。

+ #strong[第二层优化]：换机型降油耗（总时间不增且座位足够）。

== 下界推导

per-facility 强下界：每设施 $F$ 的出海与海返可共享架次，故访问 $F$ 的架次数 $>= max(ceil(n_o / 19), ceil(n_r / 19))$；穿梭每对 $(F_1, F_2)$ 至少 $ceil(n / 19)$ 趟访问 $F_1 -> F_2$。求和得 $K_"lb" = 167$，$T_"lb" = 27172$ min（452.9 h）。

== 求解结果

求解得到问题二五项指标如表4所示。

#three-line-table(
  [表4 问题二求解结果],
  (auto, auto, auto),
  ([指标], [数值], [说明]),
  (
    [总飞机使用时间], [28427 min (473.8 h)], [主目标],
    [人员总在途时间], [253195 min (4219.9 h)], [次优化],
    [总架次数], [167], [与下界一致],
    [总燃油消耗量], [231607.6 kg], [次优化],
    [座位利用率], [0.5512], [座位复用提升],
  ),
)

相对强下界的 gap 为 $(28427 - 27172) / 27172 = 4.6%$。架次数（167）与下界完全一致，表明出海与海返的座位复用接近最优。相对问题一，架次数 110 -> 167（增 52%，低于需求 150% 的增幅，得益于座位复用），座位利用率 0.4305 -> 0.5512（提升 28%），验证了往返环座位复用的有效性。
