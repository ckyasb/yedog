#import "../lib.typ": *

= 问题分析

== 数据理解

附件 `distances.csv` 给出 55×55 的对称距离矩阵（3 机场 + 52 设施，对角线为 0）。经校验，机场与设施两两间飞行距离满足对称性与非负性；机场到设施的距离范围为 153–439 km（中位 245 km），设施间距离 14–579 km（中位 157 km）。每座设施距最近机场的归属为 A01 服务 32 座、A03 服务 12 座、A02 服务 8 座。

需求文件 `peopleQ1.csv` 含 1600 条出海需求（起点 LAND×1353 + 指定机场×247，终点全为海上设施，52 个目的设施各 19–51 人）；`peopleQ2.csv` 含 4000 条，构成为出海 1600 + 海返 1600 + 穿梭 800（问题一的人员编号是问题二出海子集）；`peopleQ3.csv` 与问题二同编号同起终点，附加时间窗与任务类型，分布为常规倒班（shift）3440 条、增储上产（production）360 条、应急处置（emergency）40 条、临时任务（temporary）160 条，时间窗长度差异显著（见图3）。

#figure(
  image("../../figures/fig4_demand.pdf", width: 92%),
  caption: [各问题需求规模与问题二需求构成],
) <fig:demand>

#figure(
  image("../../figures/fig5_q3_tasktype.pdf", width: 92%),
  caption: [问题三任务类型分布与时间窗长度分布],
) <fig:q3task>

== 续航约束分析

续航是本题的关键硬约束。每个架次从机场满油起飞，可用燃油为油箱容量减去安全余油 $W_t - s_t$，不加油最大飞行路径为 $(W_t - s_t)/c_t$。往返不加油要求单程不超过最大路径的一半。各机型从最近机场往返不加油的可达性如图4所示：T1 可用燃油 850 kg、最大路径 250 km，往返不加油可达 0/52 座设施（每座设施往返均需加油）；T2 最大路径 400 km，可达 13/52；T3 最大路径 482.8 km，可达 34/52。因此机型选择与加油点插入是一阶决策。

#figure(
  image("../../figures/fig3_reachability.pdf", width: 70%),
  caption: [各机型从最近机场往返续航可达性],
) <fig:reach>

续航可行性的判定与加油点选择流程如图5所示：满油起飞后逐航段扣减油耗，若到达下一点的余油低于安全余油，则在前方可加油设施加满；若无可加油点则插入最近可达的可加油设施（受 5 次着陆上限约束），仍不可行则换大机型或拆分架次。

#figure(
  image("../../figures/fig_refuel.pdf", width: 82%),
  caption: [续航（加油）可行性决策流程],
) <fig:refuel>

== 问题一分析

问题一为纯出海运输，无时间窗、飞机充足。由于人员全部在机场上机、设施下机，机上人数随交付递减，一架次最多运 $C_t$ 人（不论去几个设施）。因此架次数下界 $K_"lb" = ceil(1600 / 19) = 85$（T3 全用）。合并多设施到一架次不降座数，但可通过 `ceil` 的超可加性降低架次数与油耗。关键决策为：人员聚类（按目的设施）、架次路径构造（每架次最多 5 站）、机型选择（座位数与油耗权衡）、续航可行性与加油点选择。

== 问题二分析

问题二在同一架次内混合三类人员，座位动态复用是核心。出海人员在机场上机、设施下机，海返人员在设施上机、机场下机，穿梭人员在设施间转运。以设施 $F$ 为中心的往返环 $A -> F -> A$ 可同时载出海（目的 $F$）与海返（起点 $F$），在 $F$ 处先下出海、再上海返与穿梭，释放的座位即时复用。穿梭人员 $F_1 -> F_2$ 需嵌访问 $F_1$ 后访问 $F_2$ 的架次。难点在于机上人数时序与座位容量约束的建模。

#figure(
  image("../../figures/fig_flight_arch.pdf", width: 90%),
  caption: [单架次出海、海返与穿梭联合运输及座位动态复用示意],
) <fig:arch>

== 问题三分析

问题三在问题二基础上叠加时间窗、24 架飞机配额、运营窗（06:00–18:00 起飞、≤20:00 返场、不过夜）、30 分钟周转与优先级约束。临时任务可取消。采用两阶段策略：阶段 A 排非临时 3840 条得总飞机使用时间 $T_0$；阶段 B 在 $T <= T_0$ 下将临时任务以零额外时间插入现有架次或新开架次，最大化满足数。24 架飞机按"机场—机型—序号"编号（如 A01-T2-H03），机队配置为 A01（T1×3, T2×3, T3×2）、A02（T1×2, T2×4, T3×2）、A03（T1×2, T2×3, T3×3），合计 24 架。
