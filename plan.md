# 方案

要依次调用这些 skill，按照里面要求完成任务。

用户偏好：
- 排版引擎：Typst（本机 `/snap/bin/typst`，CJK 字体齐全）
- 竞赛类型：2026 年度“策联杯”数学建模精英联赛（无专用模板族）
- 论文语言：中文
- 子问题数量：已知 4 个（问题1 数据整理与初步分析 / 问题2 策略参数对寿命影响 / 问题3 寿命预测 / 问题4 兼顾充电时间与寿命的策略优化）
- 模板基底：`5writing/templates/zh/default`，按策联杯《论文格式规范》裁剪

workflow:
   step      skills
1. 赛题分析与建模设计 - `2analysis-modeling`
2. 编程实现和图表生成 - `3coding-visual`
3. 流程与架构图绘制 - `4drawio`
4. 竞赛论文撰写 - `5writing`
5. 验证和验收 - `6verity`

## 题面要点（先读后做）

- 题目：A 题 锂离子电池快充策略对寿命衰减的影响建模与优化
- 数据集：MIT–Stanford 公开锂离子电池循环老化数据集，49 块 A123 18650 磷酸铁锂电池，额定容量 1.1 Ah，衰减至约 80% SOH。
- SOH = Q_current / Q_initial；**80% SOH 为寿命终止阈值**（统一）。
- 充电策略：两阶段快充——以 C1 从 0% SOC 充到 Q1，以 C2 从 Q1 充到 80% SOC，再 1C CC-CV 充满（C/50 截止）。决策参数 **C1、Q1、C2**。各电池放电策略一致，差异仅在充电策略。
- 附件：`battery_summary.csv`（49 行：battery_id、global_id、dataset_id、local_id、policy、C1、Q1、C2、initial_capacity、mean_chargetime、mean_IR、mean_Tavg、prediction_test）；`cycle_train.csv`（9350 行：battery_id、cycle、capacity、SOH、SOH_smooth、chargetime、IR、Tavg、policy）。
- 9 种充电策略，电池数 3~8 不等；**9 块测试电池**（prediction_test=1）数据截至第 150 次循环，其余 40 块截至第 200 次循环。
- 关键坑：`battery_summary.csv` 中 C1 对 `80PER_3_6C` 策略为空（该策略第一阶段无独立倍率，按 C2=3.6C 直接充），需在数据整理时说明；policy 名解析（`5_6C_19PER_4_6C_NEWSTRUCTURE` → C1=5.6, Q1=19, C2=4.6）需统一。

## 四个子问题递进链

| 子问题 | 输入 | 输出/对象 | 主要模型方向 |
| --- | --- | --- | --- |
| 问题1 | cycle_train.csv、battery_summary.csv | 整理后的策略-寿命-充电时间-内阻-温度表、SOH 曲线、长/短寿策略 | 描述性统计、可视化、寿命分布 |
| 问题2 | 问题1 整理结果 + (C1,Q1,C2) | 策略参数与寿命关系、显著性、影响程度、SOC 区间衰减特征 | 方差/非参数检验、相关/回归、因子分析（注意 C1/Q1/C2 非完全独立） |
| 问题3 | 9 块测试电池前 150 次循环 | 第 151~200 次 SOH 预测、80% SOH 时循环寿命、精度评价、早期数据长度敏感性 | 时间序列/机器学习 SOH 预测（防泄露），特征工程（IR、chargetime、Tavg、容量增量等） |
| 问题4 | 问题2 衰减规律 + 问题3 预测模型 | 兼顾充电时间与 SOH 衰减的推荐 (C1,Q1,C2) | 多目标优化（充电时间模型 + SOH 衰减模型），优先在已有实验策略范围内比较，外推限制在合理邻域 |

递进依赖：问题2 用问题1 的寿命表；问题3 的预测模型与问题2 的衰减规律共同支撑问题4 的优化。

## 格式规范约束（裁剪模板时遵守）

- 第 1 页为摘要专用页（含标题和关键词，不超过 1 页，不译英文），页码从“1”起连续编号于页脚中部。
- 第 2 页起为正文（**无目录，正文 ≤ 30 页**），正文后为附录（页数不限）。
- 附录须含：支撑材料文件列表、全部可运行源代码、AI 工具使用声明。
- 全文不得显示参赛者身份和学校信息。
- 参考文献须规范列出并在正文标注。
- 参考文献之前设置“AI 工具使用声明”（未用/已用二选一）；已用 AI 的，支撑材料需含 `AI 工具使用详情.pdf`（工具名/版本、用途环节、提示与过程、采纳与人工核验情况）。
- 提交：参赛论文（PDF/Word，≤20MB，单文件）+ 支撑材料（RAR/ZIP，≤20MB）。

## 阶段产物

- `reports/ANALYSIS_MODELING_REPORT.md`（阶段1）：变量/假设/四问模型/公式链/数据整理方案/防错要点。
- `code/` `results/` `figures/数据图` `reports/RESULTS_REPORT.md`（阶段2）：可复现代码、结果表与 JSON、数据图表。
- `figures/*.drawio` + 导出 PDF、`reports/DRAWIO_REPORT.md`（阶段3）：技术路线图、子问题求解流程图、模型结构图等非数据图。
- `paper/`（阶段4）：`main.typ` + `sections/`，按策联杯规范裁剪，图表直接插入。
- `reports/VERIFY_REPORT.md`（阶段5）：可复现性、数值一致性、格式规范、提交就绪检查。

## 风险控制

- C1/Q1/C2 非完全独立变化 → 问题2 不能简单做多因素独立回归，须用分组对比 + 部分因子/相关分析，并在报告中说明局限。
- 9 块测试电池仅前 150 循环 → 问题3 防数据泄露；用其余 40 块做训练/验证（可留出末段做伪预测检验）。
- 优化外推风险 → 问题4 先在 9 种已有策略范围内比较，外推限制在合理邻域并说明依据。
- 数值一致性 → 论文引用的所有数值必须可追溯到 `results/` 或结果 JSON，`5writing` 不得编造。
- 随机性 → 任何含随机种子的模型固定种子，并在结果中报告稳定性。
