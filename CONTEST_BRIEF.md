# 策联杯 B 题 · 赛题与数据摘要（供建模流程参考）

> 本文件整理自 `problem.pdf`（B 题题面，9 页）与 `format.pdf`（论文格式规范，2 页），汇总网络结构、机型参数、架次规则、目标函数、三问要求与结果文件格式。新会话的 `1start-mathmodel` / `2analysis-modeling` 阶段可直接参考，但仍需独立完成假设预检与建模报告。

## 一、赛题总览

**题目**：海上油田人员直升机运载计划编排。
**场景**：3 座陆地机场（A01/A02/A03）+ 52 座海上设施（F001–F052），其中 8 座可加油（F006、F011、F018、F024、F031、F038、F044、F050）。海上设施距海岸 100–250 km。人员往返由直升机运输，机场与设施两两间飞行距离见 `distances.csv`（55×55 对称矩阵，对角线为 0）。

**三类运输**：
- **出海 outbound**：陆地机场 → 海上设施
- **海返 return**：海上设施 → 陆地机场
- **穿梭 shuttle**：海上设施 → 海上设施

**业务性质与优先级**（高→低）：应急处置 `emergency` > 增储上产 `production` > 常规倒班 `shift` > 临时任务 `temporary`。临时任务可被取消以减少运输开销。

**核心链路**：编排直升机架次（route）→ 把每名人员分配到某架次的某段（assignment）→ 在最小化总飞机使用时间的同时优化座位利用率、油耗、人员在途时间。

## 二、机型参数（表 1）

| 机型 | 座位数 | 速度 km/h | 油耗 kg/km | 油箱容量 kg | 最低安全余油 kg |
|------|-------|----------|-----------|-----------|---------------|
| T1 | 12 | 250 | 3.4 | 1000 | 150 |
| T2 | 16 | 220 | 2.5 | 1150 | 150 |
| T3 | 19 | 190 | 2.9 | 1600 | 200 |

- 座位数不含飞行员/机组；速度、油耗不随载客量变化。
- 三种机型均可在全部 52 座海上设施起降，也可在 8 座可加油设施补给燃油。
- **每个架次从机场满油起飞**；到达每一处（含最终返回机场）时剩余燃油 **不得低于该机型最低安全余油**；停靠期间不另计油耗。
- 可加油设施停靠时**可选是否加油**，加油则**加满**油箱。
- 停靠最短时长：不加油 ≥ 10 min，加油 ≥ 20 min（需等待可延长）。
- 总燃油消耗 = 各航段油耗之和。
- **不考虑**机场、海上设施、加油站的并发容量限制。

## 三、架次（flight / route）规则

一架次 = 一架直升机从某机场起飞，连续停靠 1 座或多座海上设施（在各设施送下/接上人员），最后返回**原机场**；途中不得在另一座机场中转或结束。

- **最多 5 次海上着陆**（飞机离开后重返同一设施，仍按实际着陆次数累计）。
- 人员须由**同一个架次**运送，不得换乘其他直升机。
- 同一停靠**先下后上**，刚释放的座位可立即复用。
- 架次总飞机使用时间 = 从机场起飞至返回同一机场的时间，含各航段飞行与海上停靠。
- **所有时间向上取整到整数分钟**。

## 四、待运需求结构

一条需求含：`person_id`（P+4 位数字）、起点、终点、最早可登机时间、最晚需到达时间、业务性质。

- 起点/终点可为任一机场或设施；`LAND` 表示任意陆地机场（出海/海返可接受任意机场）。
- 人员所乘飞机离开起点时刻 **不得早于** `earliest_pickup_time`；抵达终点时刻 **不得晚于** `latest_arrival_time`。
- `task_type` ∈ {`emergency`, `production`, `shift`, `temporary`}。

### 数据量与构成（已核对）

| 文件 | 条数 | 构成 |
|------|------|------|
| `peopleQ1.csv` | 1600 | 全部出海（origin: LAND×1353 + A01×79 + A02×82 + A03×86；dest 全为 F0xx） |
| `peopleQ2.csv` | 4000 | 出海 1600 + 海返 1600 + 穿梭 800（Q1 是 Q2 出海子集，人员编号一致） |
| `peopleQ3.csv` | 4000 | 与 Q2 同编号同起终点，加时间窗与 task_type：shift×3440、production×360、temporary×160、emergency×40 |
| `distances.csv` | 55×55 | 含表头，机场3 + 设施52；F-F 距离 14–579 km；矩阵对称、对角线 0 |

- Q3 时间窗：`earliest_pickup` 2026-08-03 00:00 ~ 08-09 16:40；`latest_arrival` 2026-08-03 08:07 ~ 08-09 23:59；窗长 0.5–144.8 h（中位 48.2 h）。

## 五、目标函数

**主目标**：最小化**总飞机使用时间** = Σ 各架次（起飞→返回时间）。

**次要优化**（在主目标最优前提下尽量优化）：
- **人员总在途时间** = Σ 各人员（离开起点→到达终点总时长，含绕行、停靠、等待）。
- **座位利用率** = (Σ 机上人数×航段距离) / (Σ 机型座位数×航段距离)。
- **总燃油消耗量** = Σ 各架次油耗。
- **总架次数**（与飞机使用时间、机型选择联动）。

> 题面明确"在最小化总飞机使用时间的基础上尽可能优化其他因素"，即**字典序/分层目标**：先求总飞机使用时间最小，再在该最优解集中优化座位利用率/油耗/在途时间等。论文需给出五项指标：总飞机使用时间、人员总在途时间、总架次数、总燃油消耗量、座位利用率。

## 六、三问详解

### 问题 1：单向出海运输
- 输入 `peopleQ1.csv`（1600 条出海需求）。
- **不考虑**最早可登机/最晚到达/业务优先级约束。
- 假设飞机足够多。
- 目标：最小化总飞机使用时间，再优化座位利用率、油耗等。
- 输出 `q1-routes.csv`、`q1-assignments.csv`。
- 论文给出：总飞机使用时间、人员总在途时间、总架次数、总燃油消耗量、座位利用率。

### 问题 2：出海、海返与穿梭联合运输
- 输入 `peopleQ2.csv`（4000 条，出海/海返/穿梭三类混合）。
- 同样**不考虑**时间与优先级约束，飞机足够多。
- 同问题 1 目标与五项指标。
- 输出 `q2-routes.csv`、`q2-assignments.csv`。
- 难点：同一架次内三类人员混合上下机、座位动态复用、起降机场选择（LAND 可任选）。

### 问题 3：带时间要求的多日排班
- 输入 `peopleQ3.csv`（4000 条，加时间窗与 task_type，时段 2026-08-03 00:00 ~ 08-10 00:00）。
- **飞机数量限制**：24 架（表 3），规划开始时各停所属机场。
  - A01: T1×3, T2×3, T3×2（共 8）
  - A02: T1×2, T2×4, T3×2（共 8）
  - A03: T1×2, T2×3, T3×3（共 8）
  - 合计 T1×7, T2×10, T3×7 = 24
  - 编号格式 `机场-机型-序号`，如 `A01-T2-H03`。
- **运营约束**：06:00–18:00 起飞，不晚于 20:00 返回，**不得在海上设施过夜**；架次返回后≥30 min 机场周转方可再起飞。
- **临时任务可取消**：
  1. 先**不考虑**临时任务（3440 shift + 360 production + 40 emergency = 3840 条）排班，得总飞机使用时间 `T0`。
  2. 在**总飞机使用时间不超过 `T0`** 的条件下，把 160 条临时任务尽量加入，尽可能多满足临时人员运输，并尽可能优化其他因素。
- **注意**：题面要求"每名临时任务人员无论是否安排，均须在 q3-assignments.csv 保留一行；未安排时后四列全留空"。
- 输出 `q3-routes.csv`（带时刻）、`q3-assignments.csv`。
- 论文给出：临时人员运输需求满足情况、总飞机使用时间、人员总在途时间、总架次数、总燃油消耗量、座位利用率。

## 七、结果文件格式（关键，易错点）

### q1/q2 结果（问题 1、2 共用格式，Q2 多穿梭）

**`*-routes.csv`**（架次及停靠顺序）
```
aircraft_type,flight_no,stop_order,facility_id,refuel
```
- `aircraft_type` + `flight_no` 共同确定一架次；同一机型 `flight_no` 从 1 起连续。
- `stop_order` 从 0 起连续；**首、末记录须为同一座机场**；中间海上设施记录 **≤ 5 条**；所有实际停靠均须列入。
- `refuel`：1=加油（只能用于指定可加油设施），0=不加油；**机场行 refuel 必须为 0**。

**`*-assignments.csv`**（人员所乘架次及上下机位置）
```
person_id,aircraft_type,flight_no,pickup_stop_order,delivery_stop_order
```
- 每名人员一行；`pickup_stop_order < delivery_stop_order`；两序号指向同一架次，分别与该人员起点、终点一致。
- **下机序号 = 该人员上机后架次第一次停靠其终点时的序号**。
- 人员陆地端为 `LAND` 时，以所乘架次的机场作为实际起点/终点。

**示例**（T2 自 A01 经 F006、F020 返 A01；E0001 出海至 F006，E0002 F006→F020 穿梭，E0003 F020→LAND 海返）：
```
routes:  T2,1,0,A01,0 / T2,1,1,F006,1 / T2,1,2,F020,0 / T2,1,3,A01,0
assign:  E0001,T2,1,0,1 / E0002,T2,1,1,2 / E0003,T2,1,2,3
```

### q3 结果（问题 3，带时刻与具体飞机编号）

**`q3-routes.csv`**
```
aircraft_id,flight_no,stop_order,facility_id,arrival_time,departure_time,refuel
```
- `aircraft_id` + `flight_no` 定一架次；每架飞机 `flight_no` 按起飞时刻从 1 起连续。
- 架次**第一行只填 `departure_time`，最后一行只填 `arrival_time`**，两行地点均为该机所属机场；中间海上停靠同时填到达与离开时刻。
- **后一地点 `arrival_time` = 前一地点 `departure_time` + 该航段飞行时间**（飞行时间由距离/速度算，向上取整到分钟）。
- 时刻格式 `YYYY-MM-DD HH:MM`；`refuel` 规则同 Q1/Q2。

**`q3-assignments.csv`**
```
person_id,aircraft_id,flight_no,pickup_stop_order,delivery_stop_order
```
- emergency/production/shift 人员四项安排字段**均须填写**。
- **temporary 人员无论是否安排都保留一行**；未安排时后四列全留空，如 `E0004,,,,`。

## 八、数据附件清单（`data/`）

| 文件 | 内容 |
|------|------|
| `distances.csv` | 55×55 距离矩阵（首列 from_id，其余列名为到达地点），单位 km |
| `peopleQ1.csv` | Q1 出海人员 person_id, origin_id, destination_id（1600 行） |
| `peopleQ2.csv` | Q2 出海+海返+穿梭（4000 行） |
| `peopleQ3.csv` | Q3 人员起终点+时间窗+task_type（4000 行） |
| `q1-routes.csv` / `q1-assignments.csv` | Q1 结果模板（仅表头 + 全空人员行，需填充） |
| `q2-routes.csv` / `q2-assignments.csv` | Q2 结果模板 |
| `q3-routes.csv` / `q3-assignments.csv` | Q3 结果模板 |

> 模板文件目前 `*-routes.csv` 只有表头，`*-assignments.csv` 已预置全部 person_id 行（后四列空），需据此填充。

## 九、策联杯论文格式规范（与 C 题一致，要点）

1. **第一页摘要专用页**：含标题+关键词，不超过一页；页码从 1 开始（页脚居中，阿拉伯数字）。
2. **第二页起正文，不要目录，正文 ≤ 30 页**。
3. **正文后附录，页数不限**：含支撑材料文件列表、全部可运行源程序代码、AI 工具使用声明。缺源程序/不能运行/结果与论文不符 → 取消评奖。
4. **任何地方不能显示参赛者身份和学校信息**。
5. 参考文献按科技论文规范，正文引用处标注。
6. 字号字体行距颜色不统一要求。
7. 提交：参赛论文 PDF（≤20MB，不压缩）+ 支撑材料 RAR/ZIP（≤20MB）。命名 `XXX_参赛论文` / `XXX_支撑材料`（XXX 三位队号）。

**AI 工具使用规定**：参考文献之前设"AI 工具使用声明"二选一；用 AI 则支撑材料含 `AI 工具使用详情.pdf`（工具名称版本、使用目的与环节、提示方式与过程、对 AI 输出的采纳/修改/核验情况）。

## 十、建模难点与方法建议（供 `2analysis-modeling` 参考）

本题本质是**带取送货、时间窗、多机场、续航（加油）约束的丰富车辆路径问题（PDPTW + Refueling + Fleet Sizing）**，难度高，需分问分层处理。

1. **Q1（纯出海，无时间窗）**：可抽象为多机场 VRP with Deliveries。关键决策：人员聚类（按目的设施空间聚合）→ 架次路径构造（每架次最多 5 站）→ 机型选择（座位数 vs 油耗权衡）→ 续航可行性与加油点选择。可先用聚类+构造启发式（如 sweep/节约算法）得可行解，再用局部搜索/元启发式（禁忌、模拟退火、大邻域搜索 ALNS）优化。需证明总飞机使用时间最优（或给出下界）。

2. **Q2（出海+海返+穿梭）**：同一架次内三类人员混合，座位动态复用是核心。属 PDPTW（pickup & delivery with transshipment）。需建模机上人数时序、座位容量约束、起降机场配对。穿梭人员起点是上一人下机点，强耦合。建议先做需求配对（出海-海返可拼同一架次往返），再嵌入穿梭。

3. **Q3（时间窗+24 架飞机+周转+优先级+临时取消）**：多日 PDPTW with Time Windows + Fleet Scheduling。
   - 先解 3840 条非临时（shift/production/emergency），得 `T0`。
   - 再在 `T0` 约束下贪心/增量插入 160 条 temporary，最大化满足数。
   - 续航：满油起飞 + 8 个加油点 + 安全余油，需在路径上动态管理油量（可建模为节点-油量状态 DP 或加油点选择子问题）。
   - 机型与飞机编号绑定，需考虑各机场机型配置与周转。
   - 06:00–18:00 起飞、20:00 前返回、不过夜、30 min 周转是硬约束。

4. **续航约束专项**：满油起飞，每段耗 `油耗×距离`，到任意点余油 ≥ 安全余油；可在 8 点加满。可建模为带"加油决策"的最短路/资源约束子问题，或用列生成把续航可行性作为标签传播条件。

5. **取整规则**：所有时间向上取整到分钟，飞行时间 = ceil(距离/速度) 分钟，需在时刻计算中一致应用。

6. **下界与最优性论证**：建议对 Q1 给出基于下界（如总人公里/机型座位数、最短返场距离）的最优性差距分析，增强说服力。

7. **结果校验**：写独立校验脚本，检查 routes/assignments 的一致性（首末同机场、≤5 站、refuel 合法、pickup<delivery、时刻单调、续航可行、人员全分配），避免"结果与论文不符"被取消评奖。

## 十一、环境就绪状态

- 待 `1start-mathmodel` 阶段确认；预期与 C 题一致：typst 0.15.1、python3 3.12 + numpy/scipy/pandas/matplotlib/scikit-learn、pdftoppm/mutool 可用于 6verity 视觉检查。
- WSL 直连文件系统，`/mnt/c/...` 可读；工作目录 `~/yegou/B题`。
